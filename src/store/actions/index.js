// allows us to add all the different actions together into one file for easier importing
export { add, subtract, increment, decrement } from './counter';
export { storeResult, deleteResult } from './result';
